# Concatenation of Array

## Approach

Create a new array with double the original length. For every index in the new array, use `i % n` to wrap around the original array.

## Complexity

- Time: `O(n)`
- Space: `O(n)`
